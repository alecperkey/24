# See https://help.github.com/ignore-files/ for more about ignoring files.

# expo
.expo/

# dependencies
/node_modules

# misc
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# to compare environments, store sensitive info, extracting here is probably fine, right? want to avoid accidentally committing these... 
# for example if one extracted them to root, this would be problematic. better approaches?
/git-ignored